# Source — Discussion notes with Kris

**Captured:** 2026-08-13
**Format:** Not a transcript. Three bullet points captured by Jason from a live discussion with Kris and pasted verbatim into the `/prd-from-transcript` command.
**Gemini summary:** None provided for this session. There is no AI summary to verify against, so the Phase 1 "Gemini accuracy" check is a no-op — the raw capture below is the only source of truth.

---

## Raw capture (verbatim)

> not a transcript but here are three things i captured from a discussion with Kris
>
> * Add an “Updates” timeline which would allow us to log interactions that we have with the client and notes about it - for example we want to see how long it takes between touches and we should be able to log meeting, phone call, email, etc. Could it just be comments labelled updates?
>
> * Another consideration is that we are connecting tasks to the client portal and sometimes we want to create internal tasks for leads that we don’t want the client to see, how should we handle? Each task has a visibility toggle for client viewing with color coding? Should these tasks be mostly predefined - follow up, send (document, proposal, engagement letter, etc), other. This would allow us to save the task to the client and not in the general sales strategy project (that no longer exists). I don’t like the current structure of tasks in a lead adding to a hardcoded project that actually no longer exists
>
> * Make referral source info on leads into a contacts dropdown so it carries over to the project when created. Maybe source needs to be rethought altogether since it’s not really being used outside of people?

---

## Notes on fidelity

- Bullets 1 and 2 end in open questions, not decisions. Kris/Jason posed options ("Could it just be comments labelled updates?", "Each task has a visibility toggle... with color coding?", "Should these tasks be mostly predefined?") — these were resolved in the Phase 1 question round, not in the room. See the Key Decisions table in [../README.md](../README.md) for what was decided and by whom.
- Bullet 2 contains one firm, non-negotiable statement: *"I don’t like the current structure of tasks in a lead adding to a hardcoded project that actually no longer exists."* That is a defect report, not an option.
- Bullet 3's second sentence ("Maybe source needs to be rethought altogether") is exploratory. Scope boundaries for it were set during Phase 1.
