# PRD-001 Manual Test Plan

> Generated by `/autotest-prd` on 2026-08-03.
> Companion to `TEST-PLAN.md` — items checked off there by automation are still listed here in **Section C** for human gut-check.

Autotest exercised **65 of 68** test items with zero failures and zero fixes required: full browser flows as both roles (agent-browser), SQL fixture flips for every state the plan references, and real HTTP intake beacons for the D8 lifecycle. The remaining 3 items need human/external context (Section B).

## Test environment

- Internal portal: `http://localhost:3000` (client portal :3001 not touched by this PRD)
- Fixture logins: `autotest@local.test` (ADMIN), `client@test.local` (CLIENT) — passwords were reset by the autotest run via the local GoTrue admin API to `autotest-pw-2026`
- Seed data: 6-row `form_submissions` matrix (contact captured; audit in_progress / abandoned / completed / captured unread; captured acked) + 30 acknowledged abandoned rows for pagination + 2 rows created over HTTP (audit "Http Probe", contact "Contact Probe")
- ⚠️ Autotest **appended** `AUDIT_INTAKE_TOKEN` / `CONTACT_INTAKE_TOKEN` (local-only values) to `apps/internal/.env.local` to exercise the intake webhooks — keep for local beacon testing or delete freely; production values are separate
- Migration `0054_form_submission_acknowledgement` is applied locally; production apply happens at deploy

---

## 🟥 Section A — Highest priority (failed/flaky/observed issues)

**None.** Every automated assertion passed on the first attempt; no flakes, no product-decision surprises surfaced. (If the gut-check pass in Section C turns anything up, promote it here.)

## 🟧 Section B — Not automatable (require human/external context)

### Server-action direct invocation (guards verified in source + SQL, error strings not exercised)
- [ ] **B1. (03 — "Action rejected for CLIENT role / malformed UUID")** Next server actions can't be invoked by curl without crafting action-ID headers. Verified instead: `assertAdmin` + zod `uuid()` guards exist in `acknowledge-submission.ts`, and no CLIENT-reachable UI invokes the action. To verify by hand: as a CLIENT user, attempt the action via devtools (replay a POST from an admin session with the client's cookies) → expect an error response, no state change.
- [ ] **B2. (04 — "Restore non-archived / archive unknown id → friendly error")** Same server-action limitation. The mutation guards were SQL-verified (restore-on-active and double-archive both no-op). To verify by hand: trigger a stale-UI race — open the List in two tabs, archive a row in tab 1, then archive the same row from tab 2's still-rendered button → expect a destructive toast with a friendly message, not a crash.

### Real assistive tech
- [ ] **B3. (06 — "VoiceOver announces the unread count")** The accessibility tree is correct (accessible name verified as "Submissions (3 unread)"; visible pill shows `99+` while sr-only text carries the real count) — but only a real screen reader confirms the announcement. macOS: `Cmd+F5`, tab to the sidebar Submissions link, confirm it reads the label plus "(N unread)".

## 🟩 Section C — Gut-check items (passed automation, eyeball for polish)

### 02 — Page shell & tabs
- [ ] **C1.** Flip through Submissions / Archive / Activity tabs — do the tab transitions, card styling, and filters-in-tabs-row placement *feel* identical to Hour Blocks?
- [ ] **C2.** Resize the window down — tabs row wraps (`sm:` breakpoints); confirm nothing overlaps at narrow widths.

### 03 — Acknowledge & unread
- [ ] **C3.** Eyeball the unread dot + medium font weight — is the visual contrast between read/unread rows obvious enough at a glance?
- [ ] **C4.** Acknowledge from the sheet — "Acknowledged just now" swaps in place; confirm the transition doesn't jump the layout.
- [ ] **C5.** Use the Unread quick filter during a real triage session — is All/Unread the right control shape, or would a count-in-label ("Unread (3)") serve better?
- [ ] **C6.** Click the mailto link with your real mail client configured — correct address, sensible feel.

### 04 — Archive & restore
- [ ] **C7.** Archive confirm dialog copy ("stop counting toward unread… restore at any time") — right tone? Destructive-red Archive button appropriate for a recoverable action, or should it be neutral?
- [ ] **C8.** Archive → Restore round trip — confirm the row's unread state surviving feels correct rather than surprising.

### 05 — Activity
- [ ] **C9.** Read the Activity tab entries aloud — do "Acknowledged submission from Casey Prospect (casey@example.com)" style summaries read well in a real feed?
- [ ] **C10.** Load more past 20 — scroll position and button state feel right?

### 06 — Nav badge
- [ ] **C11.** Watch the badge across a triage session (acknowledge a few, archive one) — does the count update promptly enough on every navigation, or does the no-polling trade-off (D6) bite in practice?
- [ ] **C12.** Badge pill styling on the active (highlighted) nav item vs inactive — legible in both light and dark themes?

### Cross-role
- [ ] **C13.** Sign in as `client@test.local` — confirm the portal feels complete without Submissions (no dead links, home page balanced with two widgets).
- [ ] **C14.** Production smoke after deploy: migration `0054` applied, badge starts at 0 (backfill), first real marketing-site beacon lands a row.

---

## ✏️ Fixes applied during autotest

| # | Issue | File | Commit |
|---|-------|------|--------|
| — | None — all 65 automated assertions passed without code changes | — | — |

---

## Summary counts

| Section | Count |
|---------|-------|
| A — High priority follow-ups | 0 |
| B — Not automatable | 3 |
| C — Gut-check (auto-pass) | 14 |
| **Total** | **17** |
